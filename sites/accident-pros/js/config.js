/* Accident Pros Inc. — wire-up. Edit to take the demo live.
   "Request a free estimate" stays a muted "coming soon" until `primary` points at
   a real link (an estimate request form). That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_ESTIMATE_FORM_URL",
  email:   "accidentpros@yahoo.com"
};

/* Moab Tour Company — wire-up. Edit to take the demo live.
   "Check availability" stays a muted "coming soon" until `primary` points at
   a real link (booking system, inquiry form). That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_BOOKING_URL",
  email:   "moabtourcompany@gmail.com"
};

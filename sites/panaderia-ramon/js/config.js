/* Panadería Ramón — wire-up. Edit these to take the demo live.
   The hero "Order a custom cake" button stays a muted "coming soon"
   until `primary` points at a real ordering/contact link (Square, a
   form, even a Facebook order post). That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_ONLINE_ORDER_OR_CONTACT_URL",
  email:   "REPLACE_WITH_CONTACT_EMAIL"
};

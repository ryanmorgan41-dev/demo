/* Wolford Collision Repair — wire-up. Edit to take the demo live.
   "Request an estimate" stays a muted "coming soon" until `primary` points at
   a real estimate form / booking link. That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_ESTIMATE_FORM_URL",
  email:   "wolfords@brigham.net"
};

/* Moab Red Rock Bakery — wire-up. Edit to take the demo live.
   "Order ahead" stays a muted "coming soon" until `primary` points at
   a real link (online ordering, a form, a phone-order page). That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_ORDER_URL",
  email:   "MoabRedRockBakery@gmail.com"
};

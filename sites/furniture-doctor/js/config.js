/* Chay Dressmaking & Alterations — wire-up. Edit to take the demo live.
   "Book a fitting" stays a muted "coming soon" until `primary` points at
   a real booking link (Calendly, a form). That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_BOOKING_OR_CONTACT_URL",
  email:   "REPLACE_WITH_CONTACT_EMAIL"
};

/* Panadería Alicia's — wire-up. Edit to take the demo live.
   "Order a custom cake" stays a muted "coming soon" until `primary` points at
   a real link (order form, ordering system). That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_ORDER_URL",
  email:   "panaderiaalicias.site@gmail.com"
};

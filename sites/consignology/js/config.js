/* Consignology — wire-up. Edit to take the demo live.
   "See current inventory" stays a muted "coming soon" until `primary` points at
   a real link (Facebook shop, an inventory page, a form). That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_INVENTORY_OR_FACEBOOK_URL",
  email:   "info@consignology.com"
};

/* European Expressions Furniture Repair — wire-up. Edit to take the demo live.
   "Get a custom quote" stays a muted "coming soon" until `primary` points at a
   real link (quote form / photo-upload page). That gap is the pitch. The email
   is live now: it opens a message to Patrick, their real photo-to-quote flow. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_QUOTE_URL",
  email:   "Patrick@EuroEx1.com"
};

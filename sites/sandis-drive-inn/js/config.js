/* Sandi's Drive Inn — wire-up. Edit to take the demo live.
   "Order ahead" stays a muted "coming soon" until `primary` points at
   the real 3rd-party ordering widget URL. That gap is the pitch. */
window.SITE_LINKS = {
  primary: "REPLACE_WITH_ORDERING_URL",
  email:   "dineatsandis@gmail.com"
};
